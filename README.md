# Ethical Auto Totem

This repository is **source-only**: binary files are not supported and must not be committed.

Build the jar locally:

```bash
gradle clean build
```

The output jar is written to:

- `build/distributions/ethicalautototem-1.0.0.jar`
